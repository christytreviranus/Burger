<div class="mt-4">
	<h4 class="devoured-burger-name">{{burger_name}}</h4>

	<!--Throw away burger button-->
	<button class="delete-burger btn btn-danger" data-id="{{this.id}}"> Throw away</button>
</div>